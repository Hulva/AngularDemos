import { CleanPipePipe } from './clean-pipe.pipe';

describe('CleanPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanPipePipe();
    expect(pipe).toBeTruthy();
  });
});
